<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.force4us.dao.UserAddressDao">
    <select id="findUserAddressByUserIdAndState" resultType="UserAddressEntity">
        select * from user_address WHERE user_id=#{userId} AND state=#{state}
    </select>

</mapper>